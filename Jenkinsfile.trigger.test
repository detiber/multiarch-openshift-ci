node("paas-sig-ci-slave01") {
  ansiColor('xterm') {
    timestamps {
      stage('test') {
        echo 'Hello Trigger'
        sh('''#!/usr/bin/bash -xeu
          env | sort
        ''')
	echo 'Getting triggered here again'
      }
    }
  }
}
